name: Upload Repository File List
on:
  workflow_dispatch:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  list-and-report-files:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: List files in repository
        id: list-files
        run: |
          # Create temporary JSON file with the file list
          echo '{"files":[' > file_list.json
          
          # List all files, excluding .git directory, format as JSON array
          find . -type f -not -path "*/\.git/*" -not -path "*/node_modules/*" | sort | sed 's/^\.\///g' | \
          awk 'BEGIN {ORS=""} 
          {
            if (NR > 1) print ","; 
            print "{\"path\":\"" $0 "\"}"
          }' >> file_list.json
          
          echo ']}' >> file_list.json

      - name: Send file list to application
        env:
          API_ENDPOINT: ${{ secrets.API_ENDPOINT }}/files
          API_KEY: ${{ secrets.WEBHOOK_SECRET }}
          REPO_ID: ${{ github.repository_id }}
          REPO_NAME: ${{ github.repository }}
          COMMIT_SHA: ${{ github.sha }}
          BRANCH_NAME: ${{ github.ref_name }}
        run: |
          # Create and send payload
          curl -s \
            -X POST "$API_ENDPOINT" \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer $API_KEY" \
            -d "{
              \"repository_id\": \"$REPO_ID\",
              \"repository_name\": \"$REPO_NAME\",
              \"commit\": \"$COMMIT_SHA\",
              \"branch\": \"$BRANCH_NAME\",
              \"file_list\": $(cat file_list.json)
            }"